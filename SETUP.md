# 🚀 Инструкции за първоначална настройка

## 1️⃣ Инсталация на зависимостите

```bash
npm install
```

Това ще инсталира:
- Next.js 16
- Prisma
- AuthJS (next-auth@beta)
- bcrypt (за енкриптиране на пароли)
- zod (за валидация)
- lucide-react (за икони)
- TailwindCSS

## 2️⃣ Настройка на базата данни

### Създайте MySQL база данни

```sql
CREATE DATABASE amazonka CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Конфигурирайте .env файла

Копирайте `.env.example` към `.env`:

```bash
cp .env.example .env
```

Редактирайте `.env` и попълнете вашите данни:

```env
DATABASE_URL="mysql://username:password@localhost:3306/amazonka"
AUTH_SECRET="генериран-ключ-тук"
AUTH_URL="http://localhost:3000"
```

### Генерирайте AUTH_SECRET

В терминала изпълнете:

```bash
openssl rand -base64 32
```

Копирайте резултата и го поставете в `.env` като стойност на `AUTH_SECRET`.

## 3️⃣ Инициализация на Prisma

### Генерирайте Prisma Client

```bash
npx prisma generate
```

### Синхронизирайте schema с базата данни

```bash
npx prisma db push
```

## 4️⃣ Създаване на администраторски акаунт

### Вариант 1: Чрез Prisma Studio (препоръчително)

1. Стартирайте Prisma Studio:

```bash
npx prisma studio
```

2. Отворете браузъра на `http://localhost:5555`

3. Отидете на модела `User`

4. Натиснете `Add record`

5. Попълнете:
   - `name`: Admin
   - `email`: admin@artbuildshop.com
   - `password`: Използвайте bcrypt hash (вижте по-долу)
   - `role`: ADMIN

6. Запазете

### Създаване на bcrypt hash за паролата

Можете да създадете hash с Node.js:

```bash
node -e "const bcrypt = require('bcrypt'); bcrypt.hash('вашата-парола', 10, (err, hash) => console.log(hash));"
```

Или използвайте онлайн bcrypt генератор с rounds=10.

### Вариант 2: Чрез SQL

```sql
INSERT INTO User (id, name, email, password, role, createdAt, updatedAt) 
VALUES (
  'admin-id', 
  'Admin', 
  'admin@artbuildshop.com', 
  '$2b$10$вашият-bcrypt-hash-тук',
  'ADMIN',
  NOW(),
  NOW()
);
```

## 5️⃣ Стартиране на приложението

### Development режим

```bash
npm run dev
```

Отворете [http://localhost:3000](http://localhost:3000)

### Production build

```bash
npm run build
npm run start
```

## 6️⃣ Първи стъпки в админ панела

1. Отидете на `http://localhost:3000/admin/login`

2. Влезте с имейла и паролата на администраторския акаунт

3. Ще бъдете пренасочени към Dashboard

4. Започнете да добавяте:
   - Категории (Декоративни изделия, Строителни изделия)
   - Продукти
   - Блог публикации
   - Галерия изображения

## 7️⃣ Структура на категориите (препоръчана)

### Декоративни изделия
- Гипсови орнаменти
- Розетки за таван
- Гипсови корнизи
- Гипсови колони и капители
- Гипсови рамки
- Декоративни панели и 3D плоскости

### Строителни изделия
- Гипсови елементи за стени и тавани
- Гипсови профили и плочи
- Гипсови елементи за реставрация
- Промишлени гипсови компоненти

## 🔧 Troubleshooting

### Грешка: "Module not found"

```bash
npm install
npx prisma generate
```

### Грешка при свързване с база данни

Проверете:
- MySQL сървър работи ли?
- DATABASE_URL е ли правилен?
- Потребителят има ли достъп до базата?

### TypeScript грешки

```bash
npm run build
```

Това ще покаже всички TypeScript грешки.

### Prisma грешки

```bash
npx prisma validate
npx prisma format
```

## 📚 Полезни команди

```bash
npx prisma studio              # Отваря DB браузър
npx prisma db push            # Синхронизира schema
npx prisma generate           # Генерира Prisma Client
npx prisma migrate dev        # Създава миграция
npx prisma migrate reset      # Ресетва БД
npm run lint                  # Проверка с ESLint
npm run build                 # Production build
```

## 🎉 Готово!

Вашето приложение е готово! Можете да започнете да добавяте съдържание чрез админ панела.

---

За въпроси и поддръжка, вижте документацията в README.md
